# SDMART Backend (Upgrade 6)
Run:
- copy .env.example to .env and set values
- npm install
- npm run dev

APIs:
- GET /api/health
- GET /api/products?q=search
- POST /api/chat { userId, message }
- POST /api/admin/product {sku,title,...}
